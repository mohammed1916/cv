rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access, usage and billing data is owned by the authenticated backend.
    match /{document=**} { allow read, write: if false; }
  }
}
